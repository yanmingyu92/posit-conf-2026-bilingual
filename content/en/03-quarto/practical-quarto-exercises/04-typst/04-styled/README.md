# Turn 3, finished

The release with everything the module added:

- `typst` alongside `html` in the formats, and `keep-typ: true`
- the status badge, as a raw Typst block
- a set rule and a show-set rule, in a second raw block above it

Render it to compare against your own.
