# Your turn 1: render it as a PDF

1. Add `typst` as a second format in `report.qmd`. Keep `html`:

   ```yaml
   format:
     html: default
     typst: default
   ```

2. Render with **Quarto: Preview Format…** and pick Typst.

3. Render the HTML too. What came across? What didn't?
